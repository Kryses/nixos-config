{
  services = {
      syncthing = {
          enable = true;
          # group = "kryses";
          # user = "kryses";
          # dataDir = "/home/kryses/work/repos/ayon-workspace";    # Default folder for new synced folders
          # configDir = "/home/kryses/.config/syncthing";   # Folder for Syncthing's settings and keys
      };
  };
}
